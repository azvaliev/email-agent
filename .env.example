# Core database connection used by Kysely, better-auth, and background jobs.
# Format: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/email-agent

# Secret used by better-auth to sign cookies/tokens. Must be >=32 chars of high entropy.
# Example generator (macOS/Linux): `openssl rand -base64 32`
BETTER_AUTH_SECRET=

# Base URL of the deployed application, used for auth callback redirects.
# In local development this should match the Next.js dev server URL.
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth credentials from https://console.cloud.google.com/apis/credentials
# Create an OAuth client (Web) and copy the client ID and secret here.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
